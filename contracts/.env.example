# Copy to .env (gitignored — NEVER commit the real file).
# Use a FRESH throwaway wallet funded only with Base Sepolia test ETH.
PRIVATE_KEY=0xyour_test_wallet_private_key
# Optional: free Etherscan API key for --verify (works for Basescan Sepolia).
ETHERSCAN_API_KEY=
