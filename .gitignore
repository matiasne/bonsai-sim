# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Node (dev tooling only — the game itself has no build)
node_modules/
npm-debug.log*
package-lock.json
.npm-cache/

# Electron desktop app (packaged output; source in electron/ is committed)
dist/

# Foundry (contracts/) — .env holds the deploy key and must never be committed
contracts/cache/
contracts/out/
contracts/.env
